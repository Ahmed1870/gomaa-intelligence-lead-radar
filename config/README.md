The project is autonomous: discovery and sending are workflow jobs.
Do not use it to bypass CAPTCHAs, WAFs, authentication, robots restrictions, or provider policies.
The default sending policy is permissioned.
